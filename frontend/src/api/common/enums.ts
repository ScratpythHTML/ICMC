export enum Sex {
  Male = 0,
  Female = 100,
}

export enum Size {
  XXS = 0,
  XS = 100,
  S = 200,
  M = 300,
  L = 400,
  XL = 500,
  XXL = 600,
}

export enum StorageLocation {
  Imperial = 0,
  Westway = 100,
}

export enum GearCategory {
  BelayDevice = 0,
  Carabiner = 100,
  Crashpad = 200,
  Harness = 300,
  Helmet = 400,
  Quickdraw = 500,
  Rope = 600,
}

export enum MemberType {
  FullMember = 0,
  SocialMember = 100,
}
